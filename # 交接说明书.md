# TabSis (表姐) 项目交接说明书

## 一、项目概述

**项目名称**: TabSis (中文名：表姐)
**定位**: 模块化智能财务数据中台
**核心目标**: 解决传统 ERP 难以处理外部非结构化数据（如发票 Excel、银行流水 PDF）的痛点。TabSis 作为数据清洗和分析的中间层，将杂乱的外部数据转化为结构化洞察，并提供 AI 智能助手进行交互式分析。

---

## 二、技术栈

### 2.1 后端 (`tabsis-backend`)
- **语言**: Python 3.11
- **框架**: FastAPI
- **数据库**: 
  - **元数据**: SQLite (通过 Tortoise ORM 管理，存储项目/企业/模块配置)
  - **大数据分析**: Apache Doris 4.0 (存储海量交易明细，支持高并发分析)
- **AI/LLM**: 
  - **Agent**: 自研 Agent 逻辑 (`agent_handler.py`)
  - **Text-to-SQL**: Vanna.AI (用于复杂查询)
  - **OCR**: RapidOCR + PyMuPDF (用于 PDF 解析)
  - **模型接入**: OpenRouter / DeepSeek / Gemini

### 2.2 前端 (`tabsis-frontend`)
- **框架**: Vue 3 + TypeScript
- **构建**: Vite
- **UI 组件**: Ant Design Vue 4.x
- **状态管理**: Pinia
- **路由**: Vue Router
- **样式**: 自定义深色模式 (Cyberpunk/Sci-Fi 风格)

### 2.3 部署
- **容器化**: Docker Compose
- **服务编排**: 
  - `tabsis-backend`: API 服务 (Port: 8018)
  - `tabsis-frontend`: Web 服务 (Port: 5173)
  - `doris-fe`: Doris 前端节点 (Port: 8030/9030)
  - `doris-be`: Doris 后端节点 (Port: 8040/9050)

---

## 三、系统架构与目录结构

### 3.1 目录结构
```
e:\doris - caicai\
├── tabsis-backend/           # 后端服务 (原 doris-api)
│   ├── main.py               # API 入口
│   ├── db.py                 # Doris 连接池
│   ├── metadata_db.py        # SQLite 元数据管理
│   ├── metadata_models.py    # ORM 模型 (Project, Company)
│   ├── invoice_handler.py    # 发票模块逻辑 (Excel清洗+SQL聚合)
│   ├── bank_handler.py       # 银行流水模块逻辑 (PDF解析+AI分析)
│   ├── agent_handler.py      # AI Agent 逻辑 (MCP协议支持)
│   └── ...
├── tabsis-frontend/          # 前端服务 (原 doris-frontend)
│   ├── src/
│   │   ├── layouts/MainLayout.vue  # 主布局 (含 Agent 侧边栏)
│   │   ├── views/
│   │   │   ├── ProjectHub.vue      # 项目列表
│   │   │   ├── CompanyDashboard.vue# 企业看板
│   │   │   └── modules/            # 功能模块视图
│   │   │       ├── InvoiceModule.vue
│   │   │       └── BankModule.vue
│   │   ├── stores/                 # Pinia 状态
│   │   └── router/                 # 路由配置
│   └── ...
├── docker-compose.yml        # 整体部署配置
└── README.md                 # 项目说明
```

### 3.2 数据库设计
1.  **SQLite (Metadata)**:
    -   `projects`: 项目基础信息
    -   `companies`: 企业信息 (含背景描述)
2.  **Doris (Data)**:
    -   **隔离策略**: 每个项目一个独立 Database (如 `p_1_audit_2024`)
    -   **表命名**: `c_{company_id}_{module}_{timestamp}`
    -   **存储**: 采用 Doris 的列式存储，支持亿级数据秒级查询。

---

## 四、功能模块详解

### 4.1 发票分析模块 (Invoice Analysis)
-   **输入**: 标准化发票导出 Excel (`Al发票.xlsx`)
-   **处理**: 
    -   自动映射列名 (购买方、销售方、金额、税额等)
    -   清洗数据 (去除空行、格式化日期)
    -   导入 Doris 临时表
-   **输出**: 5 张核心报表 (信息汇总、销项按商品/客户汇总、进项按商品/供应商汇总)

### 4.2 银行流水模块 (Bank Statement)
-   **输入**: 银行对账单 PDF / Excel
-   **核心技术**: 
    -   **OCR**: 提取文本坐标
    -   **AI Vision**: 使用多模态大模型 (Gemini/GPT-4o) 识别表格结构
    -   **逻辑校验**: `上期余额 + 收入 - 支出 = 本期余额` (自动标记异常行)
-   **输出**: 结构化交易明细表 (存入 Doris)

### 4.3 AI Agent "表姐" (Cousin Agent)
-   **交互**: 侧边栏悬浮聊天窗口 (可缩放)
-   **上下文**: 自动感知当前所在的项目和企业
-   **MCP 协议 (Model Context Protocol)**:
    -   Agent 不仅返回文本，还能返回结构化 JSON (`widget_type: 'table'/'chart'`)
    -   前端自动渲染为可视化组件，而非单纯的文字表格。

---

## 五、API 接口建设

### 5.1 项目与企业管理
-   `GET /api/projects`: 获取项目列表
-   `POST /api/projects`: 创建项目
-   `GET /api/projects/{id}/companies`: 获取项目下的企业
-   `POST /api/companies`: 创建企业

### 5.2 模块功能
-   `POST /api/modules/invoice/upload`: 上传发票 Excel 并分析
    -   参数: `file`, `project_id`, `company_id`
-   `POST /api/modules/bank/upload`: 上传银行流水 PDF 并分析
    -   参数: `file`, `project_id`, `company_id`

### 5.3 Agent 交互
-   `POST /api/agent/chat`: 发送消息给 Agent
    -   参数: `message`, `project_id`, `company_id`
    -   返回: `{ message: "...", widget: { type: "table", data: [...] } }`

---

## 六、后续维护建议

1.  **AI 模型配置**:
    -   在 `docker-compose.yml` 或环境变量中更新 `OPENROUTER_API_KEY`。
    -   建议使用支持 Vision 的模型 (如 `google/gemini-2.0-flash-exp`) 以获得最佳的 PDF 解析效果。
2.  **前端优化**:
    -   目前图表 (ECharts) 仅预留了占位符，建议后续完善 `MainLayout.vue` 中的 Chart 渲染逻辑。
3.  **数据备份**:
    -   定期备份 `doris-fe-meta` 卷和 SQLite 数据库文件。
